# h3 Tiler

⚠ WIP ⚠

To run the client in `map/` run:

```bash
npm install
```

then:

```bash
npm start
```

to spin the tiler, in `src/h3tiler`, run:

```bash
python -m uvicorn app:app --reload
```
